/** Hourly player-facing feature log. Read this before adding the next one. */

export const FEATURE_LOG = [
  "Tab or T cycles the gold hunt lock to the next living beast already in the nearby enemy pool (no world scan).",
] as const;
